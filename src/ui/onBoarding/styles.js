import {StyleSheet} from 'react-native';
import colors from '../../theme/colors';


const styles = StyleSheet.create({
  dotStyle: {backgroundColor: colors.transparent},
  backgroundImage: {width: '100%', height: '100%'},
});

export default styles;
